# S0 Go/No-Go 评审记录

## 状态

- 评审状态：真实设备验证进行中
- 暂定结论：NOT_READY
- 评审日期：2026-08-02（阶段记录）
- 主测试设备：Redmi 23078RKD5C（corot），Android 13 / API 33，MIUI 14 V14.0，安全补丁 2023-10-01
- 对照设备：待填写
- Android 9 实体机：待填写
- 时间线与统计报告：原始时间线仅本地留存；当前无偏校准见[延迟真值报告](results/miui14-23078rkd5c-v010-deferred-truth-record-only-stable10-20260802.report.json)

NOT_READY 表示证据尚未齐备，不代表已经技术否决。

## 工程基线

- [x] PoC 三 APK 可编译
- [x] 无障碍与 UsageStats 双时间线已实现
- [x] Android 9 兼容事件映射有单元测试
- [x] 状态机、身份闸门和 S0 测试包硬边界有单元测试
- [x] 一次 Back、一次可选 Home、动作后验证已实现
- [x] 标注真值、JSONL 导出和 Wilson 报告工具已实现
- [x] Redmi MIUI 14 记录模式稳定自动 100 次基线完成
- [x] Redmi MIUI 14 无障碍重连单次探针完成
- [x] Redmi MIUI 14 延迟真值 10 次校准完成
- [x] Redmi MIUI 14 记录模式混合负样本 15 次与桌面断路探针完成
- [ ] Redmi MIUI 14 动作、允许、手动及负样本完整矩阵完成
- [ ] 对照设备完成
- [ ] Android 9 实体机完成

## 指标

| 指标 | 成功/总数 | 点估计 | Wilson 95% | 门槛 | 结论 |
| --- | ---: | ---: | ---: | ---: | --- |
| 目标事件获取率 | 10/10 | 100% | 72.25%-100% | ≥95% | 延迟真值校准点估计通过；样本不足 |
| 应阻止候选识别率 | 10/10 | 100% | 72.25%-100% | ≥93% | 延迟真值校准点估计通过；样本不足 |
| 允许流程误阻止率 | 0/15 | 0% | 0%-20.39% | ≤2% | 仅记录模式预检；不能替代武装样本 |
| 最终离开目标成功率 | 0/0 | 不可计算 | 不可计算 | ≥90% | 未武装、未执行动作 |

| 延迟 | P50 | P95 | 备注 |
| --- | ---: | ---: | --- |
| 候选生成 | 110 ms | 140 ms | 延迟真值 10 次；目标 ≤150 ms |
| 首次动作 | 不可计算 | 不可计算 | 未武装、未执行动作 |
| 最终离开目标 | 不可计算 | 不可计算 | 未武装、未执行动作 |

## 安全否决项

- [ ] 无无限 Back/Home 循环
- [ ] 无关键系统界面动作
- [ ] 身份未知时无动作
- [x] 主动回桌面后旧来源上下文不沿用（记录模式单次探针）
- [ ] 允许流程误阻止可控
- [ ] 撤销任一能力后安全降级

任一安全否决项不通过，不得给出 Go。

## 能力结论

### 稳定

- 在本机解除电池限制、允许自启动并由用户明确启动前台服务后，静置 35 分钟仍保持前台保活状态；
- v0.0.10 延迟真值 10 次校准中，组合实时事件与候选均为 10/10；目标退出后才由来源广播原始进入时刻，避免目标侧真值唤醒观察进程；
- 无障碍主信号在延迟真值校准中为 10/10，候选延迟 P95 140 ms；
- 设置、Chrome、桌面各 5 次的记录模式负样本为 0 候选、0 动作、0 安全违规；桌面后直接打开 Chrome 的单次探针未复用旧来源上下文；
- 重连后历史 UsageStats 事件只记录为预热、不驱动候选；随后单次跳转为 1/1；
- 本轮未观察到安全违规、ANR 或广播超时。

### 不稳定但可工程化缓解

- 约 2.1 秒节奏的短间隔压力 10 次中，仅 2/10 在 500 ms 内到达；其余事件在 MIUI Greeze 解冻后约 48-59 秒才出现。稳定基线改用 6 秒间隔可以避免本机 5 秒冻结窗口，但真实应用的突发连续拉起仍是明确风险；
- UsageStats 在历史 v0.0.6 稳定批次只有 49/100 实时到达；v0.0.10 延迟真值校准为 0/10 实时、最终 10/10，不能作为单独的实时动作依据；
- v0.0.10 对超过 500 ms 的信号失败开放，并在 UsageStats 与实时无障碍窗口冲突时清空上下文、只用实时窗口快照恢复；
- 前台服务、自启动和电池无限制是当前 MIUI 基线条件，仍需验证重启、用户划掉任务、长待机与电量影响。

### 公开 API 下不可实现

- 无法获得来源应用发出的原始 Intent；
- 无法获得 Android 公共 API 提供的权威“调用者 → 目标”调用链；
- 普通无 Root 模式无法在 `startActivity()` 之前通用拦截。

## 最终决策

- [ ] Go：进入 S1/S2
- [ ] Conditional Go：列出前置条件、负责人和期限
- [ ] No-Go：停止完整产品开发并记录原因

当前保持 `NOT_READY`：延迟真值校准与记录模式负样本预检的点估计达到阶段要求，但尚未授权执行真实 Back/Home，也没有武装允许 200 次、手动 100 次、完整上下文负样本、对照设备和 Android 9 证据。上述项目完成前不得勾选 Go。

补充证据：

- 用户中断的 65 次部分批次仅作本地诊断留档，不计失败；
- [无障碍重连探针](results/miui14-23078rkd5c-v006-accessibility-reconnect-probe-20260802.report.json)；
- [短间隔压力报告](results/miui14-23078rkd5c-v005-unrestricted-fgs-autostart-idle35-record-only-auto10-eventual-20260802.report.json)；
- [记录模式混合负样本报告](results/miui14-23078rkd5c-v010-testsource006-record-only-negative15-20260802.report.json)；
- [桌面后 Chrome 上下文断路报告](results/miui14-23078rkd5c-v010-home-then-chrome-context-break-probe-20260802.report.json)。

技术负责人：待签署

QA 负责人：待签署

产品负责人：待签署
